function test(input) {
	let test = input[1];
	let krai = Number(input.length - 1);
	let index = 0;
	if (test * 1 === test) {
		index++;
	}

	console.log(test);
}
test([ 10, 'Done', 2, 'Done', 20, 'Done', 20, 122, 'Done' ]);
